#[cfg(test)]
mod tests {
    // use arena::Valued as Arena;
    // use attribute::Attribute;
    // use matrix::Related as Graph;
    // use state::State;

    pub mod resources {
        // use std::path::PathBuf;

        // pub fn system() -> PathBuf {
        //     resource::path::system().join("graph/module/")
        // }
    }

    #[test]
    fn boolean() {
        // let _matrix: Graph<State<usize>> = serialization::file::read(resource::file::Meta::json(
        //     resources::system()
        //         .join("math/numeric/logic/boolean/boolean.forge.relation.artifact.json"),
        // ));
        // let arena: Arena<Attribute<String>> =
        //     serialization::file::read(resource::file::Meta::json(
        //         resources::system().join("math/numeric/logic/boolean/boolean.arena.json"),
        //     ));
        // let state = State::initialize(&[26, 25, 10]);
        // let states = States::initialize(&[state]);
        // let step = lava::transition(&matrix, &states);
        // serialization::file::write(
        //     resource::file::Meta::json(resources::system().join(
        //         "math/numeric/logic/boolean/generated/boolean.forge.iteration.artifact.json",
        //     )),
        //     &step,
        // );
    }
}
